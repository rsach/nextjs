// pages/info.tsx
import InfoPage from '../components/informationPage';

// Info page that displays the paginated list from the GraphQL API
const Info = () => {
  return (
    <div>
      <InfoPage />
    </div>
  );
};

export default Info;
